version: "3"

services:
  5etools:
    # build: .
    # image: 5etools-custom
    image: ghcr.io/5etools-mirror-3/5etools-img:latest
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      # - ./data:/var/www/localhost/htdocs/data/
      - .:/var/www/localhost/htdocs/